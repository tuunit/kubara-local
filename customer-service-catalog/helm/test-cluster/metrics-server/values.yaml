namespace:
  labels:
    project-name: "test-cluster"
    stage: "local"
metrics-server:
  imagePullSecrets:
    - name: image-pull-secret
  serviceMonitor:
    additionalLabels:
      monitoring:
        instance: "test-cluster-local"
