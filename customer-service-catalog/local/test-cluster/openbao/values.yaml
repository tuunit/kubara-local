server:
  dev:
    enabled: true
    devRootToken: root
  ingress:
    enabled: true
    ingressClassName: traefik
    activeService: false
    hosts:
      - host: openbao.172.18.0.3.traefik.me
        paths:
          - /
  extraEnvironmentVars:
    BAO_DEV_LISTEN_ADDRESS: "0.0.0.0:8200"
injector:
  enabled: false
ui:
  enabled: true
